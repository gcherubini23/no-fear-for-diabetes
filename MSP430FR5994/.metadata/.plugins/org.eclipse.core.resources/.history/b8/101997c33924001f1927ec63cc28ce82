#include <msp430.h>
#include <stdio.h>
#include <stdbool.h>
#include <math.h>
#include <time.h>
#include "memory_util.h"
#include "config.h"


bool sample_measurement(float* z) {
    *z = 110;
    time = lr(&secondsElapsed) / SEC_IN_MIN;
    if (time >= UPDATE_RATE) {
            return TRUE;
    } else {
        return FALSE;
    }
}

bool sample_input(float* u) {
    u[CHO] = 0;
    u[IIR] = 0;
    time = lr(&secondsElapsed) / SEC_IN_MIN;
    if (time >= UPDATE_RATE) {
            return TRUE;
    } else {
        return FALSE;
    }
}
